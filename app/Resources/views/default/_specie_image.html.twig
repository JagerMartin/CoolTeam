
{% if picture is not null %}
    <img src="{{ absolute_url(asset(vich_uploader_asset(picture, 'imageFile'))) }}" alt="{{ picture.imageName }}" />
{% else %}
    <img src="{{ absolute_url(asset('img/no-photos.png')) }}"  alt="Avatar vide" />
{% endif %}
